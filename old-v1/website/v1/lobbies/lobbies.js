(()=>{const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));async function join(id,code=''){const request=window.FourWeirdServer?.request;if(!request)return alert('Sign in to join a lobby.');const r=await request('/api/lobbies/'+id+'/join',{method:'POST',body:{join_code:code}});alert(r.success?'Joined! Opening the game…':r.error||'Unable to join.');if(r.success)location.href='/games/html/platform-wars/?match='+encodeURIComponent(r.match_id)}async function load(){const request=window.FourWeirdServer?.request;if(!request){$('#rows').textContent='Sign in to browse and join lobbies.';return}const game=$('#game').value,r=await request('/api/lobbies?all=1'+(game?'&game='+encodeURIComponent(game):''));if(!r.success){$('#rows').textContent=r.error||'Unable to load lobbies.';return}const list=(r.lobbies||[]).sort((a,b)=>(a.relay_ping_ms??999)-(b.relay_ping_ms??999));$('#rows').innerHTML=list.length?list.map(l=>`<div class="row"><span><b>${esc(l.title)}</b><br><small>${esc(l.game_slug)} · @${esc(l.host_handle||'player')} · ${esc(l.visibility)}</small></span><span><span class="tag">${Number(l.relay_ping_ms)||999} ms relay</span> <button data-id="${l.id}">Join</button></span></div>`).join(''):'No open lobbies match this filter.';$('#rows').querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>join(b.dataset.id))}$('#refresh').onclick=load;$('#game').onchange=load;const q=new URLSearchParams(location.search);if(q.get('join'))join(q.get('join'),q.get('code')||'');else load()})();
