{"created": "2026-09-15T03:45:00.000Z", "gates": ["node scripts/verify-migration-versions.mjs", "self-grep: every listed signature has a REVOKE line; .rpc usage verdicts in log"], "goal": "Revoke anon (and authenticated where no legit user-JWT call; check app .rpc usage) on clan-write functions: create_clan(text,text,text), create_clan(text,text,text,text), set_clan_type(uuid,text), join_clan(uuid), create_post(uuid,text,text,text,text), create_post(uuid,text,text,text,text,text), create_comment(uuid,text,text), create_comment(uuid,text,text,uuid), file_report(text,uuid,text,text), moderate_set_status(text,uuid,text), vote_clan_post(uuid,smallint), vote_clan_comment(uuid,smallint), create_clan_channel(uuid,text,text,text,text), add_clan_channel(uuid,text,text,text), create_clan_event(uuid,text,text,timestamptz,uuid), create_clan_quest(uuid,text,integer), complete_clan_quest(uuid,uuid), create_clan_role(uuid,text,text), assign_clan_role(uuid,uuid,uuid), set_clan_member_role(uuid,uuid,text), set_clan_message_pin(uuid,boolean), set_clan_prune_settings(uuid,boolean,integer,integer,text), delete_clan_message(uuid), edit_clan_message(uuid,text), post_clan_message(uuid,text,text,uuid,text), mark_channel_read(uuid), toggle_clan_reaction(uuid,text), deploy_clan_bot(uuid,text,text), remove_clan_bot(uuid,uuid), clan_member_cap(uuid), clan_minute_rate(uuid), clan_roster_page(uuid,integer,timestamptz,uuid), clan_scale_status(uuid), clan_supporter_status(uuid), clan_tribute_status(uuid), clan_leaderboard(uuid), clan_is_moderator(uuid,uuid), log_clan_ai_usage(uuid,text,numeric), log_clan_transfer(uuid,integer,text). Skip already-revoked (grep first). Done = migration 20261219000004_security_revoke_clan.sql + green gate.", "id": "DS-SECWARN-05", "lane": "infra", "log": ["2026-09-15T03:45:00.000Z secwarn-lead: envelope created, awaiting claim.",
    "2026-09-15T04:00:00.000Z secwarn-05: claimed (in_progress, owner=secwarn-05). Grepped migrations: 38/39 sigs already revoked (locations in migration header); only clan_member_cap(uuid) had NO revoke (SECURITY DEFINER helper, no app .rpc caller, internal SQL callers unaffected). Created 20261219000004_security_revoke_clan.sql with that one revoke + per-sig audit header.",
    "2026-09-15T04:00:00.000Z secwarn-05: .rpc verdicts — all app calls via app/api/** user-JWT/anon server client (lib/supabase/server), zero direct browser .rpc in components/. No legit client call for clan_member_cap -> full revoke (public,anon,authenticated). roster/scale/supporter/tribute keep anon+authenticated grants (public clan reads need them); prune_settings keeps authenticated (POST scale route); mark_channel_read revoke present so skipped per rule. Self-grep coverage: 39/39 sigs have a REVOKE line (missing=0).",
    "2026-09-15T04:00:00.000Z secwarn-05: GATE verify-migration-versions is RED on PRE-EXISTING cross-lane version collisions (e.g. 20261219000000 x3: linter/seclint seclint/sqlint quads; 20261219000004 already x2 before my file) — steward/QUEUE rename needed, out of my write scope (migration file only). My file follows convention and adds no new KIND of failure; self-grep gate GREEN."], "owner": "secwarn-05", "scope": ["v2/vcw4w/supabase/migrations/20261219000004_security_revoke_clan.sql"], "status": "in_progress", "title": "Secwarn 05: revoke clan-write RPCs", "updated": "2026-09-15T03:45:00.000Z"}