# DevSwarm Memory (v2/vcw4w)

World-readable under `public/swarm`. NEVER put secrets, keys, tokens, or emails here.

Source of seeded lessons: `aiorch-01.md` at repo root (the prior swarm bus — history stays there, not copied here).
Cap: keep this file under ~200 lines. Summarize quarterly (fold old bullets into one line per theme).

## New entries (newest first)

- 2026-09-14 vocseed-lead: 9-agent VOCSEED CHEAP wave closed beside done sibling R-wave (QUEUE 434-435) with zero clobbers — my bank/seed-bar names are disjoint from their pools/controls names (git status ?? x9 proves NEW). Lesson: on a crowded tree, read the QUEUE TAIL (not just head) before minting — the R-wave close-out sat at line 434 and I found it only at integrator time; envelopes were still collision-free because scopes were NEW-file paths, but the convergence ruling could have been filed at dispatch. Also: node --check rejects .ts on Node 22 (use --experimental-strip-types), and repo eslint is infra-broken (ESLint 10 vs plugin) so scope-gates must say that explicitly. (evidence: TASKS/DS-VOCSEED-*.json + QUEUE vocseed-lead lines + verify-vocrehab-seeds 13/0/1).

- 2026-09-14 b2-04: Build2 rebuild green — polled DS-BUILD2-01..03 from disk to terminal (all done: drill client-island split, 3 pro-route + 2 pro-page dynamic-line deletes), then npm run build exit 0 (Compiled successfully 7.3s, tsc clean, 568/568 static pages, zero NEW errors so zero content writes). Lesson: poll-then-build with disk re-reads beats snapshots — sibling states flipped open->done mid-poll and the first read lied; never mark done over red, never hand-edit STATUS counts (recount requested via QUEUE). (evidence: TASKS/DS-BUILD2-04.json log + QUEUE close-out + build tail).

- 2026-09-14 d-lead: ss2 CHEAP D-wave (RUN-MU0MDHMR, 7 Task agents, 6 NEW vocrehab files landed eslint-clean + D8 2-line lint fix). Three cooperation lessons: (1) envelope IDs are claimable slots, not owned branches — a sibling lead repurposed my D1..D7 mid-wave for verification follow-ups and my agents' done-evidence survived only in reports + QUEUE; mint follow-ups under NEW ids, and as lead re-verify gates yourself at close (my re-run caught D1's 2 reds + confirmed D2/D4/D5 clean). (2) cross-agent contracts must be byte-pinned in every brief (D1/D3/D4 shared exact export lines — zero integration failures across 7 parallel agents). (3) static checkers need a false-positive log from day one (button-onClick, task-knowledge sk- substring) or honest gaps drown in noise. Stale CLAIM locks on repurposed envelopes must be released by the tag owner (done for D1). (evidence: QUEUE D-LEAD2 lines + RUNS/done/RUN-MU0MDHMR.json + verify-vocrehab 6/6 re-run).

- 2026-09-14 cheap-lead: 7-agent vocrehab CHEAP wave closed cooperatively beside 3 sibling crews (lead/C/D/V envelopes) — 3 done (03 games, 05 decide, 07 export+docs), 3 zero-clobber stand-downs (02/04/06 reverted duplicates, kept reusable drafts in logs), 1 honest block (01 vs C7 verifier). Lead repairs: BOM-strip torn 03 envelope (PowerShell BOM breaks TASKS JSON — write BOM-less), in-memory verified one-char FIX500-02 repair spec filed for steward. verify-vocrehab.mjs 6/6 green on re-run; pointers stay single-writer 08 Wave-4. Lesson: on a crowded tree, stand-down + revert + log beats merge-fights; envelope claims must be re-read from disk, prompts go stale mid-flight. (evidence: TASKS/DS-VOCREHAB-*.json + QUEUE cheap-lead lines + verifier output).

- 2026-09-14 vocrehab-lead: three-crew VocRehab convergence held with zero clobbers. Sibling ss2 crew (C1-C7, RUN-MU0MDHMR) owns the full module build; my 01-07 plan-series stood down to blocked-backup (01/03 honestly claimed mid-flip by voc-cheap-01/03 — live owner wins, dual-build flags filed). Verifier slot crowded (c7 overwrote mine) — accepted theirs as canonical, filed adoption nits. Lesson: when parallel crews mint overlapping series (01-07 vs C1-C7), the lead's job flips from fan-out to integrate+QA+route-findings; verify-vocrehab GREEN with tsc RED is the expected mid-wave state, and cross-slice contract drift (libs-vs-pages export names) is the #1 integration risk — name the source of truth early (evidence: QUEUE vocrehab-lead lines + tsc output).

- 2026-09-14 vocrehab-lead: VocRehab 7-agent CHEAP fan-out opened (DS-VOCREHAB-01..07 open/unowned, 08 integrator in_progress) from vocrehab-IMPLEMENTATION-PLAN.md §17; ss2 READY.json absent so classic BRAIN boot per ss2 fallback. Race-free rules: file-disjoint scopes, no cross-slice imports, builders never touch shared manifests, sibling AIs may claim any open envelope. (evidence: TASKS/DS-VOCREHAB-*.json + QUEUE tail).

- 2026-09-14 bld-05: DS-BUILD wave closed (01/02/03 done, 04 blocked-honest, npm run build 498/498 green). Two torn TASKS envelopes found mid-poll (BUILD-02 overwritten by 03 bytes, 404-05 missing log-array bracket) - both would red verify-devswarm JSON parse; steward-repaired + parse-verified, 3 more diagnosed read-only (FIX500-02/GQA-05/MMOMV-03, unescaped quotes in done logs) and QUEUEd. Lesson: re-read envelopes from disk (never trust an early snapshot) before every gate - sibling states flipped open->done mid-poll and the first read lied. (evidence: DS-BUILD-05 log + QUEUE close-out).

- 2026-09-13 search-lead: 7-agent site-search fan-out closed 7/7 done (DS-SEARCH-01..07). Builder/verifier shard-layout mismatch (shard-NNN.json vs shards/ dir) survived both landings because each gated only its own file - lead realigned the verifier and re-ran to 11/0/0. Live dev-boot curl proved the chain (API 200 real hit, page 200). Lesson: sibling-contract interfaces need a lead-owned integration gate that exercises the JOINT (boot + curl), not just per-file gates. Evidence: verify-search 11/0/0, tsc exit 0.

- 2026-09-14 oct-10: OCT-wave poll 5/9 terminal (01-05 done-green, 06/07 landed-on-disk but still in_progress, 08 in_progress on audit overlap, 09 open) — integrator stays in_progress, never done over red. Untracked-tree lesson: files on disk are NOT envelope state (06/07 look done by `git status` but their envelopes never flipped). Adjacent-lane bytes in your scope's files (.env.example touched by DTOP-audit under OCT-09's unclaimed scope) need a single-writer ruling BEFORE claiming, not a rebase after. Greps clean (no key values, bridge free of budget imports, mount verifiably unapplied). (evidence: TASKS/DS-OCT-*.json + QUEUE oct-10 lines + git status).

- 2026-09-13 dtop-lead: 10-agent desktop sweep closed 10/10 (opencode CLI in desktop code + terminal virtual-desktop attach, heal loop with token budget, coin quotes via API+MCP, audit with 14 arch decisions + pairing spec). Verifier 21/21, tsc scope-clean. 3 duplicate-agent races resolved no-clobber (01 adopted, 02/03/05 sibling-closed). Lesson: zero-pad fan-out tags (dtop-2 vs dtop-02 collided). (evidence: QUEUE tail + DS-DTOP-* logs).

- 2026-09-14 musdub-lead: 10-agent ModFX+dubstep+modes wave (DS-MUSDUB-01..10): 9 done, 09 blocked honest-extrinsic (verify-game-bundles red on 4 foreign html bytes, zero wave files named). Pattern that held: parallel fan-out with a re-dispatchable integrator — agent 10 correctly blocked when siblings were still in flight, then musdub-10b wired maker-shell green on re-dispatch. Contract neutrality worked: duck-typed bridge (zero imports from format-4w/music-format) kept the wave out of the 4W-1 vs $music:1 fight entirely. Lesson: when the integrator's deps are other in-flight envelopes, dispatch it last or plan one re-dispatch; and a blocked-on-extrinsic envelope with its own gates green is a correct outcome, not a failure (evidence: QUEUE DS-MUSDUB-LEAD close-out + both music verifiers green).

- 2026-09-14 gqa-lead: 10-agent full-catalog QA (DS-GQA-01..10, all done, 37 game dirs + 2 API lanes): pattern that held was bridge-ownership clarity — v1 game.js files NEVER post ready themselves (injected runtime-bridge owns the {version:1} handshake), so 30+ games passed with zero edits; real bugs clustered at the edges (orphaned bundles with no catalog row, hyphenated slugs, dead gravegain5d bridge, 429-via-dbFail returning 500, kind-scoped cheat reads). Two process lessons: (1) game.json slug==dir needs a verifier lint (4 silent mismatches caught by hand across 2 batches, fixed in opposite directions pending steward canon ruling); (2) lead live-smoke via `npx next dev -p <free-port>` + public-route curls (health green, authed routes fail-closed) is cheap and worth it — but never let lane agents start dev servers (port clash), and always re-check stray node processes after (evidence: QUEUE GQA-LEAD line + envelope logs).

- 2026-09-13 dtop-lead: claimed DS-DTOP-01..09 + created/claimed DS-DTOP-10 (10-agent desktop sweep: opencode CLI in desktop code + 4weird.com/terminal virtual-desktop attach, bugtest-autofix-retest loop with token budget, coin pay via MCP/API quote link, future-proof audit). Desktop already has opencode_bridge + MCP + heal_worker foundations — envelopes extend, never rewrite. Lesson: check for an existing open series before creating envelopes — DS-DTOP matched the user ask 9/10, only the audit needed creating. (evidence: TASKS/DS-DTOP-*.json).

- 2026-09-13 mob-lead: 8-agent phone-readiness fan-out closed 6 done + 2 accurately-blocked (DS-MOB-01..08). Extrinsic reds evaporate: the site-header mojibake blocking MOB-07 was gone on re-read (transient mid-write collision), leaving only pre-existing eslint reds that are SSR-unsafe to quick-fix (isKidsMode reads window.localStorage unguarded, so lazy-init would crash SSR) - left per one-owner rule. Stray root scratch files from other lanes (musdub6-check.ts) break repo tsc for everyone; report, never delete. Evidence: verify-mobile.mjs 7 pass/1 advisory/0 fail exit 0; tsc zero music+mobile errors in wave scopes.

- 2026-09-13 spaceship-lead: 6-agent move /spaceships -> /game/spaceships closed (DS-SPACESHIP-00..05 all done): route app/game/spaceships + 44 assets to public/game/spaceships + 7 nav hrefs + redirects/sitemap/canonical + docs. Two lead follow-ups in done scopes: embed script src -> game/spaceships/main.js (asset move had 404'd it) + spec boundary line. Lesson: an asset-dir move silently 404s any root-level loader HTML referencing it — grep the loader (not just the moved tree) before closing the move.

- 2026-09-13 mvaf-lead: 10-agent movement+audio sweep closed — 8/10 done (GG3D WASD mirror fixed + buzz killed, all GraveGains + neon + arcade batches audited, per-game fixes/N-As). 09 blocked (bundles red on our 3 audio fixes vs old-v1 lock + foreign template-demo), 10 blocked (3 FAILs are audited-N/A games, verifier amend requested). Repaired torn DS-MVAF-02.json. Lesson: byte-locked bundle verifiers turn any legit audio fix red — scope the waiver BEFORE the sweep next time (evidence: QUEUE tail + DS-MVAF-09 log).

- 2026-09-13 musf-lead: 6-agent music fix-wave green (DS-MUSF-01..06, all done): gallery orphan adopted with envelope disclosure, verifier exit 0 via additive legacy-pass (never delete checks on contested files), API rewired to sidecar seeds with teen-clean + 400s, 12 one-shot SFX pack, help/bot-cookbook, live dogfood 2 songs HTTP 200 — which exposed the real gap: live site runs stateless $music:1 while the repo is 4W-1, so bot submissions validate but never publish; needs deploy-convergence + DB migration, not more content (evidence: QUEUE DS-MUSF-LEAD line + verifier output + live POST/GET pair).

- 2026-09-13 ftr-lead: 10-agent footer sweep (DS-FTR-01..09 data columns + DS-FTR-10 integrator): 9/9 columns landed green first try using a frozen FooterData shape + exact-href ownership lists per envelope (zero inter-agent collisions). Integrator (single writer on site-footer.tsx) went blocked on 1 dead href, fixed by an authorized one-line repair. Then a sibling's mid-flight /mmorpg->/mmo rename (incomplete: no index pages, stale self-refs) red-lit 5 footer hrefs that were verified-green at build time. Lesson: footer/link work must re-verify hrefs at INTEGRATION time, not just build time, when route renames are in flight; post-close extrinsic reds go to QUEUE as rulings, never as silent re-lands (evidence: QUEUE FTR-LEAD line + verify-no-dead-links output).

- 2026-09-13 mvaf-lead: opened+claimed DS-MVAF-01..10 (movement camera-true WASD + buzz kill across GraveGains + all games; GG3D root causes proven pre-dispatch: mirrored trig in player.js/game-runtime.js, endless 55Hz saw in sound-engine.js). All scopes html/-only after discovering sync-game-bundles rm+copy wipes generated dirs (which also took the G4DC series + time-travel work — parked to blocked with evidence + incident filed). Lesson: check sync direction BEFORE scoping any game-bundle work; generated dirs are write-only-through-generator. (evidence: TASKS/DS-MVAF-*.json).

- 2026-09-13 music-integrator: wiring pass fulfilled both crews' QUEUE asks in one edit batch (sitemap MUSIC + docs/music x4, site-nav x2, package.json verify:music + verify:music-4w chain slots, sync injection for fourweird-music.js across 36 bundles, NEW api/music/list route, dropped build-breaking `export const dynamic` no other route uses). Proof bar for multi-crew waves: tsc/eslint/verifiers are necessary but NOT sufficient — only `npm run build` caught the illegal segment export. Pre-existing reds must be proven extrinsic via stash (template-demo/game.js) not assumed. Lesson: integrator proves extrinsics, never inherits them silently (evidence: build exit 0, 11 music routes in table).
- 2026-09-13 music-lead: 10-agent fan-out closed 10/10 (DS-MUSIC-01..10 all done). Mid-wave contract flip (Variant-B rewrite vs dispatched Variant-A) red-lit 5 dependents; fixed by supervisor convergence to the done envelope contract (16 seeds converted, verifier re-mirrored, fallbacks/presets/lab migrated) instead of per-file fights. Lessons: (a) when two tags claim one envelope, on-disk done + gates beat dispatch text - converge to disk truth, never re-rewrite; (b) a mis-edit in another lane file (4W song-card) must be reverted immediately, scope beats good intentions; (c) triple-crew shared paths need steward single-winner rulings BEFORE any crew rewrites - until ruled, extend additively + fail-open, never overwrite. Evidence: verify-music.mjs OK 16 seeds, tsc zero music errors, all seeds validateMusic-ok.


- 2026-09-13 mus-1130-lead: 10-agent music-suite wave landed files but lost its envelope IDs — sibling mus-lead rewrote DS-MUS-01..10 (13:05Z, non-padded mus-1.. owners, MIDI 4W-1 contract) over my 11:30Z envelopes (padded mus-01.. owners, step-grid spec) while my agents were in flight; a third DS-MUSIC-* crew + a converge lead also filed. All 10 of my agents still landed collision-free (verified on disk: lib/music 4W core, maker+gallery+API+widgets, fourweird-music.js bridge, 21 transcribed seeds). Lesson: envelope IDs are first-come-first-served — when two leads pick the same prefix, the later rewrite silently orphans the earlier wave's claims; leads must grep TASKS for live prefixes AND recent close-out lines before minting IDs, and on collision the dispossessed lead files disk-truth supplements (never re-mints, never flips others' envelopes) (evidence: QUEUE DS-MUS-1130 line + git status untracked music tree + verifier exit 1 on cross-crew $music:1 vs 4W-1 seeds).

- 2026-09-13 mus-converge-lead: triple-crew music-suite collision (mus-lead DS-MUS-01..10 vs music-m* DS-MUSIC-01..10 vs my 13:02Z numeric-tag wave on the SAME DS-MUS-* IDs, superseded 13:05Z in untracked tree). 8/10 of my agents stood down with zero writes (no-claim-steal held); 2 landed additive green files (lib/music/synth.ts tsc/eslint clean, public/games/shared/music-runtime.js node--check clean) now awaiting single-winner rulings vs synth-4w.ts/music4w.js/fourweird-music.js in QUEUE. Sibling verify-music.mjs 43/43 green; normative contract is 4W-1 (8192/1024), my 2048/320 spec retired without fight. Lesson: same-ID re-dispatch across leads is a guaranteed collision — leads must grep TASKS for the goal keyword AND check owner tags before minting IDs, and numeric vs hyphenated owner tags (mus-01 vs mus-1) need a registry, not memory (evidence: QUEUE converge lines + verifier 43/43 + agent stand-down reports).

- 2026-09-13 gg5d-lead: 7-agent GraveGain5D theme sweep done (DS-GG5D-01..07): golf-flavored 5D made series-true (RPG combat + races/classes + undead/Compact/MoonRock/Hades lore) ADDITIVELY because verify-gravegain5d locks class keys/HOLES/hop/paradox tokens. File-disjoint scopes held vs live DS-MMORPG-5D05 (index.html + mmorpg-5d.js excluded per SwarmStart step-5). verify-gravegain5d 43/43 green; tsc clean in 5D scope. Lesson: reframe locked flavor tokens additively, file token cleanup via QUEUE (evidence: QUEUE GG5D-LEAD line + verifier output).

- 2026-09-13 g4d-theme-lead: claimed DS-G4DC-01..07 as 7-agent fan-out (owners g4d-c1..c7) for 4D combat/RPG theme parity per user order; filed QUEUE ruling G4DC-vs-G4D3 (both -01s claim window.GraveGain4DCombat — single winner before wiring) + shell-wiring request + time-travel foundation note (alternates shim, REWIND_COST, reverseTime/deathReverse). Lesson: two same-theme series = global-collision until ruled, even with disjoint file scopes (evidence: TASKS/DS-G4DC-01 + DS-G4D3-01 goals).

- 2026-09-13 secfix-lead: 7-agent sec sweep (4 done/1 blocked/2 correct-STOPS on live SEC2 owners). Real fixes: age-verify CSRF band-planting closed, easydnc coin-path CSRF closed, unknown-slug fail-open closed on all 3 play entries, Retry-After on 3 money 429s. Lesson: the secret-scanner RED was a false positive (agent describing a PEM-stub test in its own envelope log) — reword the log, never weaken the scanner; and STOPS on live owners are successes, not failures (evidence: QUEUE SECFIX close-out + both sec verifiers green).

- 2026-09-13 g4d3-lead: same user order double-fanned AGAIN (remastery/ownership precedent): my DS-G4D3-01..07 (html combat/rpg/gore mods + bestiary/arsenal/quests/codex content, 7/7 done green) vs sibling DS-G4DC-01..07 (bundle-tree combat/rpg, unlanded per their own red verifier). Filesets fully disjoint — the ONLY collision is the window.GraveGain4DCombat global. Lesson: never close-supersede landed green work for unlanded claims; both files stand, steward rules the shared global BEFORE either side wires it, wiring waits (evidence: QUEUE G4D3 close-out line + 31/31 + 39/39 verifier runs).

- 2026-09-13 gg4d-lead: 10-agent GraveGain4D canon-mirror sweep (DS-GG4D-01..10, all done): 4 html modules (math/graphics/worlds/missions, vanilla IIFE + GraveGainMods.push + node --check) + bundle + content modes/lore + play shell + docs + verifier (31/31 green) + integration notes; tsc clean. Two cross-crew collisions filed openly in QUEUE, no silent re-lands: (1) game.js slot — DS-G4D-03's 40970B dream-slice version superseded on disk by GG4D-05's 35269B (untracked tree, git cannot recover; extend-by-merge proposed as game-golf.js variant); (2) verifier slot — GG4D version won, DS-G4D-09 already blocked, endorsed dream-checks re-land as verify-gravegain4d-dream.mjs. Lesson: when 3 crews share one bundle dir, file-disjoint scopes must be checked against ALL live envelope prefixes (G4D vs GG4D vs GRAV4D), not just your own — string-similar prefixes collide silently (evidence: QUEUE tail + verify-gravegain4d 31/31 + git status untracked-only).

- 2026-09-13 food-sweep/lead: 15-agent GraveGain food run (DS-FOOD-00 done + DS-FOOD-01..15 + DS-FOOD-INDEX blocked-extrinsic). Pattern that held: lead writes the shared contract + full roster FIRST (one writer), batch agents only spread roster entries (never retype emoji codepoints) + deriveFoodStats for numbers + drop notes — merge harness then validated 127/127 coverage with EXACT emoji union. Two encoding lessons: (1) rare codepoints mangle silently through normal writes — my 🍄‍ (U+1F344 U+200D) landed as U+100001 and needed a codepoint-explicit repair script + a check script asserting exact sequences; (2) PowerShell Set-Content -Encoding UTF8 emits a BOM that breaks the JSON verifier — batch-05 hit it, fix is BOM-less rewrite. Lesson: any task moving non-BMP/ZWJ/VS16 characters needs a machine codepoint audit, not eyeballing (evidence: DS-FOOD-00 log + merge harness ALL VALIDATE CLEAN).

- 2026-09-13 mmo-a15/infra: DS-MMO-15 integrator pass (in_progress, not done — DS-MMO-02/03/04/08/12 claimed-live, 05/06/09/10/11/13/14 open, 01 blocked-superseded). Wired QUEUE-224 + MMO-07 nav requests additively (4 docs/mmo + /games/servers + /games/servers/rent into sitemap.ts + site-nav.ts + docs-data.ts, eslint clean, sitemap §2 green for all 6); deferred games.ts catalog + GAME_RATINGS rows for ABSENT files (wiring would break catalog-runtime); ruled 4 known collisions extend-by-merge/convergence (both trees stand, mmo-age.ts canonical). MMO-scope gates green (gravegain-mmorpg OK, age-gate OK, migrations OK 109 files); npm test red ONLY on 3 foreign issues (FOOD-05 BOM breaks devswarm JSON parse, /bouncer missing from sitemap, vcw [section] closed-catalog). STATUS recounted true 08:30Z (159 envelopes: open 7/claimed 7/in_progress 3/blocked 61/done 81). Lesson: re-list TASKS + re-read shared manifests immediately before every write — DS-MMO-07 landed done + 4 docs/mmo pages appeared mid-pass, and STATUS.json was steward-recounted under me twice. (evidence: TASKS/DS-MMO-15.json log + QUEUE tail + verifier outputs).

- 2026-09-13 mmo-15/steward: MMORPG merge watch — 9 DS-MMORPG-* envelopes (2 done, 4 claimed/in_progress, 3 adjacent done) with path-disjoint scopes, zero byte collisions; wiring index filed (1d/2d shell tags + nav/sitemap for /mmorpg + /docs/mmorpg/** still open, no verify-mmorpg.mjs exists, charge_mmorpg_minutes RPC pending economy review). verify-devswarm RED on missing envelope fields (WEB-01/HUD-01 title, ECON07 scope) — live owners repair additively, steward never edits live envelopes. STATUS recounted true 07:55Z (107 envelopes: open 5/claimed 2/in_progress 2/blocked 34/done 64); several envelope stamps postdate the system clock, so order by disk content not stamps. Lesson: no DS-MMORPG-STEWARD15.json existed so no claim was taken — a steward sweep without an envelope stays open in QUEUE text rather than breaching scope (evidence: QUEUE tail + verify-devswarm output).

- 2026-09-13 sec-sweep-lead: 7-lane security sweep fanned out (DS-SEC-*-01): real fixes — matches POST age-gate, vcw empty-scope fail-closed, bot-login CSRF cookie gap, desktop BOT_RE parity+CSP+VNC+image-allowlist, clan revenue-mint 401+cap, docs privacy-safety truth-fix, infra RLS 00003 + SSRF numeric-IP fix. Sibling duplicate DS-SEC-* envelopes converged collision-free, rulings in QUEUE. STATUS recounted (open 5/claimed 0/in_progress 0/blocked 34/done 55).
- 2026-09-13 sec-infra-stale: briefed-envelope superseded mid-task (07:05 6-path DS-SEC-INFRA-01 replaced on disk by 07:10 narrow version, my claim lines overwritten in untracked tree). Lesson: re-read the envelope immediately before EVERY write, not just at claim — treat any envelope as replaceable until close, never flip an envelope another live session owns (sec-games line-141 precedent: queue findings + request ruling, stand by). Verifier-mandated envelope repairs (missing updated/trailing-comma/shifted lane-scope) are steward-safe and unblock everyone's gates; full 6-gate chain green + STATUS recounted true 07:33Z (evidence: QUEUE tail + verify-security-sweep.mjs).

- 2026-09-13 swarm/lead: DS-REMASTER-SWARM-01 created (open, docs lane, scope remastery/**) so any agent SwarmStarts the remastery folder and mini-leads the next unbuilt slice via child envelopes. Caution: several envelopes + STATUS.json carry future .000Z stamps while the system clock reads ~04:48Z — never trust `updated`/counts from cache, recount TASKS from disk before every gate read (evidence: STATUS 05:55:00.000Z vs [DateTime]::UtcNow 04:48:19Z).

- 2026-09-13 remastery-boot: lead pivoted my claimed W3-ECON to swarmstarting the remastery folder — released ECON untouched (zero scope writes) back to open/null and landed DS-REMASTER-START-01 (`remastery/SwarmStart.md`: poll STATUS, oldest open DS-REM-*/DS-REMASTER-* envelope, claim, section map §1-8→waves→prefixes→sec*.py, generator discipline README-never-hand-edit, route alias via QUEUE.md). Lesson: pivot-by-release is clean only when nothing was written — verify scope paths absent before releasing (evidence: START-01 log, verify-devswarm OK 50 tasks).

- 2026-09-13 wave2/3 triage: with zero claimable tasks open, triaged README 7.2/7.3 into 17 lane envelopes (DS-REMASTER-W2/W3-*, open/unowned, new-files-only scopes checked disjoint) and worked exactly one (W2-WEB1 DPS dashboard, 3 file agents, done). Other agents claimed 7 within minutes and renamed/reshaped some (W2-DISCORD, W3-DICTATE/MOGUL/INTEROP) — triage envelopes are seeds, later renames win, never fight them (evidence: verify-devswarm OK 49 tasks, STATUS recomputed from TASKS dir twice mid-flight).

- 2026-09-13 remastery W1: 7-lane fan-out (DS-REM-01..07, owners rem-*) landed Wave-1 slices (tools suite, mod-manifest+bridge, desktop parity board, squad-pools ledger-quote API, debug-play analyzer, remastery docs, Wave-1 SQL) — all 7 done, verify-devswarm + lane verifiers + tsc green (evidence: envelope logs). Cross-crew races with R1-R7: R7 caught spec-vs-repo drift (squads ARE teams/team_members — pre-push fix, never trust spec table names); R3 bridge + R6 overwrite show untracked-tree races need single-winner rulings, never silent re-lands; STATUS.json decays under concurrency so recount-from-TASKS before every gate read; PowerShell Set-Content writes BOM that breaks the JSON verifier — use BOM-less UTF-8.

- 2026-09-13 remastery/ownership: two sessions under owner tag `muse-spark-r4` double-fanned-out 7 file agents each into the same `lib/remastery/*` scope (envelope DS-REMASTERY-WAVE1-01). No loss because every agent did read-before-write (second pass merged/extended; one spec-violating fetch-based pipeline sketch got replaced with the fail-open engine). Lesson: owner tags must be unique per session (e.g. `muse-spark-r4b`), and a `done` envelope does not stop a second session that already adopted it — check `status` + `updated` immediately before dispatch, not just at boot (evidence: DS-REMASTERY-WAVE1-01 log lines 04:12/04:13/04:24).

- 2026-09-13 docs/wave1-guides: R6 landed 5 Wave-1 guide pages under app/docs/remastery/ (eslint clean) but overwrote DS-REM-06's same-path time-tracking/page.tsx in git-untracked tree — lesson: list the target tree immediately before first write when 10+ agents are concurrent, and file the collision openly for steward ruling instead of silently keeping the winner (evidence: TASKS/DS-REMASTER-W1-DOCS.json log + QUEUE.md conflict line).

- 2026-09-13 skill-audit followup: secret-scan regex `/sk-[A-Za-z0-9]{8,}/` false-positived on "task-marketplace" (sk- + 11 chars, mid-word) in a sibling swarm file. Lesson: secret patterns need a left-boundary guard + realistic length floor — now `/(?<![A-Za-z0-9_-])sk-[A-Za-z0-9-_]{20,}/` (evidence: verify-devswarm.mjs + full npm test green). Also deduped the double `verify:battlesharks2` key/invocation in package.json (test chain 55→54 invocations; skill.md §10 updated to match).
- 2026-09-13 parity: desktop-can-do-anything-web-can split into 4 lane envelopes (vcw shell, games catalog API, desktop native client, docs) � new files only, STATUS recomputed from TASKS dir at merge (disk had moved under us: 02:30 snapshot vs read snapshot), verify-devswarm + game-bundles + desktop green (evidence: TASKS/DS-*-01.json logs).

- 2026-09-13 desktop/exe: DS-EXE-KEY-01 done — lib.rs now accepts 20-32 suffix to match BOT_KEY_RE; gates node --check JS_OK + cargo check Finished (evidence: TASKS/DS-EXE-KEY-01.json log).
- 2026-09-13 desktop/exe: JS widened BOT_KEY_RE to {20,32} while Rust is_valid_bot_token still enforced exactly 30 chars, causing SAVE-then-reject in the exe (integration report §1). Lesson: align validation on both sides of a Tauri invoke boundary in ONE envelope before rebuilding (evidence: DS-EXE-KEY-01).
- (none yet — append new dated bullets here, newest on top; format `- YYYY-MM-DD <lane/tag>: <lesson> (evidence: <verifier/log>)`)

## Seeded lessons — 2026-09-13 (from aiorch-01.md)

- 2026-09-13 claim-before-write + re-read shared state before writing: a stale all-open snapshot led G6 to overwrite sub-03's landed `gravegain1d-art.js`; file was untracked so sub-03's bytes were unrecoverable from version control (aiorch-01.md:210, CONFLICTS:244). Lesson: CLAIM first, pull/re-read the bus + target file immediately before writing, never write from a cached snapshot.
- 2026-09-13 filename/global collisions need an orchestrator ruling BEFORE wiring: `window.GraveGain1DArt` owned by both `gravegain1d-art.js` and `gravegain-1dart.js`; `window.GraveGain25D` by `gravegain-2p5d.js` + `gravegain-25d.js`; models split `gravegain-models-3d.js` vs `gravegain3d-models.js` (aiorch-01.md:234, 245, 247-249; QUEUE:83-86). Lesson: on collision, record in CONFLICTS and wait for ruling — do not wire either side unilaterally.
- 2026-09-13 merge-vs-replace rulings live in CONFLICTS and are binding: E20 first wired exactly-one 1D-art file per dispatch, then REVERTED to the orchestrator MERGE ruling on finding it in-tree (both art files routed, merge guards, no content bytes touched) (aiorch-01.md:238-239, 248-249). Same pattern for arsenal/emergent/perf rulings (aiorch-01.md:283-285). Lesson: dispatch text loses to in-tree CONFLICTS rulings.
- 2026-09-13 generated bundles vs tracked sources discipline: `scripts/sync-game-bundles.mjs` regenerates bundles; treasure-hunters fix was generated-bundle-only (CHROME_PATTERNS regex + three.js fallback), tracked sources untouched, parity green (aiorch-01.md:108-109); sync proven deterministic over x3 runs (aiorch-01.md:219); E20 touched only sync + regenerated output (aiorch-01.md:238). Lesson: fix generators, never hand-edit generated outputs.
- 2026-09-13 done means gates green with evidence in the log: protocol requires claim → code → log line → done + verifier evidence (aiorch-01.md:6-17); e.g. plusFiles wave 44/44 green + 35 bundles + parity intact (aiorch-01.md:107); swarm verifier 123 pass / 5 fail, all 5 pre-existing content gaps, zero E20-caused (aiorch-01.md:238). Lesson: no `done` without pasted gate output.
- 2026-09-13 small scoped files beat big rewrites: content lanes create NEW files only, wiring lanes edit manifests (aiorch-01.md:13-15; ownership table:48-71); preferred fixes were additive — +21-line BIG-UPGRADE block (aiorch-01.md:202), `weaponFor` bridge instead of second arsenal runtime (aiorch-01.md:283), `pruneDecals`/`rect` compat aliases on perf v2.0.0 instead of restore (aiorch-01.md:285). Lesson: bridge/shim/append, don't rewrite a landed lane file.
- 2026-09-13 remastery concurrency: 10+ agents landing at once means STATUS.json/TASKS truth shifts mid-task — re-list TASKS immediately before every claim and close, and treat a parallel DS-REM-* split as complementary when scopes are file-disjoint (DS-REMASTERY-WAVE1-01 lib/remastery/* vs DS-REM-01..07 app/lib/migration paths). One non-owner log line landed in our envelope mid-run; ownership + scope discipline kept it harmless. (evidence: TASKS/DS-REMASTERY-WAVE1-01.json log).
- 2026-09-13 crawl-first beats static audit for 404 work: a static href-vs-page.tsx diff found 1 dead link (/login) but missed 9 runtime-500 sidebar routes whose page.tsx files existed; a live dev-server crawl (all menu hrefs + status codes) plus dev-log module errors gave true ground truth. Lesson: always boot the app and curl every menu href before scoping a 404 fix; one broken server module (app/code/page.tsx client hooks) can poison EVERY route in dev, so when all routes 500 with the same body text, fix the named module first and re-curl before scoping per-route repairs.
