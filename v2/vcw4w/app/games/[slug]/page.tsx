import Link from 'next/link'; import { notFound } from 'next/navigation'; import { games,getGame } from '@/content/games';
export function generateStaticParams(){return games.map(g=>({slug:g.slug}))}; export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const g=getGame((await params).slug);return g?{title:`${g.title} | 4weird`,description:g.description}:{}};
export default async function GamePage({params}:{params:Promise<{slug:string}>}){const g=getGame((await params).slug);if(!g)notFound();return <main className="mx-auto max-w-5xl px-6 py-16"><Link href="/games" className="text-sm text-cyan-400">← All games</Link><h1 className="mt-10 text-5xl font-bold">{g.emoji} {g.title}</h1><p className="mt-5 text-xl text-muted-foreground">{g.description}</p><Link href={`/games/${g.slug}/play`} className="mt-8 inline-flex rounded-full bg-cyan-400 px-7 py-3 font-bold text-black">Play game</Link><p className="mt-12 text-muted-foreground">A hand-crafted HTML5 experience. The original game runtime is preserved intact.</p></main>}
