import { notFound } from 'next/navigation'; import { games,getGame } from '@/content/games'; import { GameRuntimeFrame } from '@/components/games/game-runtime-frame';
export function generateStaticParams(){return games.map(g=>({slug:g.slug}))}; export default async function PlayPage({params}:{params:Promise<{slug:string}>}){const g=getGame((await params).slug);if(!g)notFound();return <main className="min-h-screen bg-black"><div className="flex items-center justify-between px-4 py-3 text-white"><a href={`/games/${g.slug}`}>← {g.title}</a><span className="text-xs text-white/60">Original HTML runtime</span></div><div className="h-[calc(100vh-52px)]"><GameRuntimeFrame slug={g.slug} title={g.title} src={g.runtimePath} /></div></main>}
