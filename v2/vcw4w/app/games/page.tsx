import Link from 'next/link'; import { games } from '@/content/games';
export const metadata={title:'Games | 4weird',description:'Play weird, wonderful browser games from 4weird.'};
export default function GamesPage(){return <main className="mx-auto min-h-screen max-w-6xl px-6 py-16"><p className="text-sm uppercase tracking-widest text-cyan-400">4weird arcade</p><h1 className="mt-3 text-5xl font-bold">Games</h1><div className="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{games.map(g=><Link key={g.slug} href={`/games/${g.slug}`} className="group rounded-2xl border bg-card p-6 transition hover:-translate-y-1 hover:border-cyan-400"><div className="text-5xl">{g.emoji}</div><h2 className="mt-5 text-xl font-semibold group-hover:text-cyan-400">{g.title}</h2><p className="mt-2 text-sm text-muted-foreground">{g.description}</p><div className="mt-5 flex flex-wrap gap-2">{g.tags.map(t=><span key={t} className="rounded-full border px-2 py-1 text-xs">{t}</span>)}</div></Link>)}</div></main>}
