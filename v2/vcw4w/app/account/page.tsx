import Link from "next/link"; import { redirect } from "next/navigation"; import type { Metadata } from "next"; import { createClient } from "@/lib/supabase/server"; import { hasEnvVars } from "@/lib/utils"; import { AccountDashboard } from "@/components/account/account-dashboard"; import { AccountHub } from "@/components/account/account-hub"; import { CheckoutControl } from "@/components/account/checkout-control"; import { DailyClaim } from "@/components/account/daily-claim"; import { ReferralPanel } from "@/components/account/referral-panel"; import { LogoutButton } from "@/components/logout-button";
export const metadata: Metadata = { robots: { index: false, follow: false } };
export const dynamic="force-dynamic";
export default async function AccountPage(){if(!hasEnvVars)return <main className="min-h-screen bg-slate-950 text-white"><section className="mx-auto max-w-4xl px-5 py-20"><h1 className="text-4xl font-black">Your account</h1><p className="mt-4 text-slate-300">Sign-in is not configured on this deployment yet. Add Supabase variables from <code>.env.example</code>.</p><Link className="mt-10 inline-block text-cyan-300 hover:underline" href="/games">Back to games</Link></section></main>;const supabase=await createClient();const {data}=await supabase.auth.getClaims();if(!data?.claims)redirect("/auth/login?next=/account");return <main className="min-h-screen bg-slate-950 text-white"><section className="mx-auto max-w-4xl px-5 py-20"><div className="flex flex-wrap items-center justify-between gap-4"><div><h1 className="text-4xl font-black">Your account</h1><p className="mt-4 text-slate-300">Signed in as {String(data.claims.email??"player")}.</p></div><LogoutButton/></div><div className="mt-10 space-y-8"><AccountDashboard/><DailyClaim/><ReferralPanel/><CheckoutControl/><AccountHub/></div><Link className="mt-10 inline-block text-cyan-300 hover:underline" href="/games">Back to games</Link></section></main>}
