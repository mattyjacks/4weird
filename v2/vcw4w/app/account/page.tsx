import Link from "next/link"; import { redirect } from "next/navigation"; import type { Metadata } from "next"; import { Suspense } from "react"; import { createClient } from "@/lib/supabase/server"; import { hasEnvVars } from "@/lib/utils"; import { AccountDashboard } from "@/components/account/account-dashboard"; import { AccountHub } from "@/components/account/account-hub"; import { AlphaTesterClaim } from "@/components/account/alpha-tester-claim"; import { CheckoutControl } from "@/components/account/checkout-control"; import { RefundPanel } from "@/components/account/refund-panel"; import { DailyClaim } from "@/components/account/daily-claim"; import { LoveWalletPanel } from "@/components/love/love-wallet"; import { ReferralPanel } from "@/components/account/referral-panel"; import { LogoutButton } from "@/components/logout-button"; import { BudgetControls } from "@/components/budget/budget-controls";
export const metadata: Metadata = { robots: { index: false, follow: false } };
// NOTE: no 'use cache' anywhere here — per-user session + Supabase reads.
// Static shell (title/copy/skeleton) prerenders; ALL session-bound content
// streams inside <Suspense> so personal data is never cached.
function AccountSkeleton(){return <div aria-busy="true" aria-label="Loading your account" className="mt-10 space-y-8 animate-pulse"><div className="h-6 w-48 rounded bg-white/10" /><div className="h-40 rounded-2xl border border-white/10 bg-white/[.04]" /><div className="h-40 rounded-2xl border border-white/10 bg-white/[.04]" /><p className="text-sm text-slate-400">Loading your account…</p></div>}
async function AccountBody(){if(!hasEnvVars)return <section className="mx-auto max-w-4xl px-5 py-20"><h1 className="text-4xl font-black">Your account</h1><p className="mt-4 text-slate-300">Sign-in is not configured on this deployment yet. Add Supabase variables from <code>.env.example</code>.</p><Link className="mt-10 inline-block text-cyan-300 hover:underline" href="/games">Back to games</Link></section>;const supabase=await createClient();const {data}=await supabase.auth.getClaims();if(!data?.claims)redirect("/auth/login?next=/account");return <section className="mx-auto max-w-4xl px-5 py-20"><div className="flex flex-wrap items-center justify-between gap-4"><div><h1 className="text-4xl font-black">Your account</h1><p className="mt-4 text-slate-300">Signed in as {String(data.claims.email??"player")}.</p></div><LogoutButton/></div><div className="mt-10 space-y-8"><AccountDashboard/><BudgetControls title="Personal monthly budget"/><AlphaTesterClaim/><DailyClaim/><LoveWalletPanel/><ReferralPanel/><CheckoutControl/><RefundPanel/><AccountHub/></div><Link className="mt-10 inline-block text-sm text-cyan-300 hover:underline" href="/games">Back to games</Link></section>}
export default function AccountPage(){return <main className="min-h-screen bg-slate-950 text-white"><Suspense fallback={<section className="mx-auto max-w-4xl px-5 py-20"><h1 className="text-4xl font-black">Your account</h1><p className="mt-4 text-slate-300">Your saves, coins, and settings live here.</p><AccountSkeleton/></section>}><AccountBody/></Suspense></main>}
