import Link from "next/link"; import { redirect } from "next/navigation"; import type { Metadata } from "next"; import { Suspense } from "react"; import { createClient } from "@/lib/supabase/server"; import { hasEnvVars } from "@/lib/utils"; import { AccountDashboard } from "@/components/account/account-dashboard"; import { AccountHub } from "@/components/account/account-hub"; import { AlphaTesterClaim } from "@/components/account/alpha-tester-claim"; import { CheckoutControl } from "@/components/account/checkout-control"; import { RefundPanel } from "@/components/account/refund-panel"; import { DailyClaim } from "@/components/account/daily-claim"; import { LoveWalletPanel } from "@/components/love/love-wallet"; import { ReferralPanel } from "@/components/account/referral-panel"; import { LogoutButton } from "@/components/logout-button"; import { BudgetControls } from "@/components/budget/budget-controls";
export const metadata: Metadata = { robots: { index: false, follow: false } };
// NOTE: no 'use cache' anywhere here — per-user session + Supabase reads.
// Static shell (title/copy/skeleton) prerenders; ALL session-bound content
// streams inside <Suspense> so personal data is never cached.
function AccountSkeleton(){return <div aria-busy="true" aria-label="Loading your account" className="mt-4 space-y-3 animate-pulse"><div className="h-5 w-40 rounded bg-white/10" /><div className="grid gap-3 lg:grid-cols-2"><div className="h-24 rounded-xl border border-white/10 bg-white/[.04]" /><div className="h-24 rounded-xl border border-white/10 bg-white/[.04]" /><div className="h-24 rounded-xl border border-white/10 bg-white/[.04]" /><div className="h-24 rounded-xl border border-white/10 bg-white/[.04]" /></div><p className="text-xs text-slate-400">Loading your account…</p></div>}
async function AccountBody(){if(!hasEnvVars)return <section className="mx-auto max-w-5xl px-4 py-6"><h1 className="text-2xl font-black">Your account</h1><p className="mt-2 text-sm text-slate-300">Sign-in is not configured on this deployment yet. Add Supabase variables from <code>.env.example</code>.</p><Link className="mt-4 inline-block text-cyan-300 hover:underline" href="/games">Back to games</Link></section>;const supabase=await createClient();const {data}=await supabase.auth.getClaims();if(!data?.claims)redirect("/auth/login?next=/account");return <section className="mx-auto max-w-5xl px-4 py-6"><div className="flex flex-wrap items-center justify-between gap-2"><div><p className="text-[11px] font-bold uppercase tracking-widest text-cyan-300">Settings console</p><h1 className="mt-1 text-2xl font-black">Your account</h1><p className="mt-1 text-xs text-slate-400">Signed in as {String(data.claims.email??"player")}.</p></div><LogoutButton/></div><div className="mt-4 grid gap-3 lg:grid-cols-2"><div className="min-w-0"><AccountDashboard/></div><div className="min-w-0"><BudgetControls title="Personal monthly budget"/></div><div className="min-w-0"><AlphaTesterClaim/></div><div className="min-w-0"><DailyClaim/></div><div className="min-w-0"><LoveWalletPanel/></div><div className="min-w-0"><ReferralPanel/></div><div className="min-w-0"><CheckoutControl/></div><div className="min-w-0"><RefundPanel/></div><div className="min-w-0 lg:col-span-2"><AccountHub/></div></div><div className="sticky bottom-0 z-10 mt-4 flex flex-wrap items-center gap-2 rounded-xl border border-white/10 bg-slate-950/90 px-2 py-1.5 text-xs backdrop-blur"><span className="font-bold text-slate-200">Account</span><span className="text-slate-500">saves, coins, settings — edits save inside each panel</span><Link className="ml-auto text-cyan-300 hover:underline" href="/games">Back to games</Link></div></section>}
export default function AccountPage(){return <main className="min-h-screen bg-slate-950 text-white"><Suspense fallback={<section className="mx-auto max-w-5xl px-4 py-6"><p className="text-[11px] font-bold uppercase tracking-widest text-cyan-300">Settings console</p><h1 className="mt-1 text-2xl font-black">Your account</h1><p className="mt-1 text-xs text-slate-400">Your saves, coins, and settings live here.</p><AccountSkeleton/></section>}><AccountBody/></Suspense></main>}
