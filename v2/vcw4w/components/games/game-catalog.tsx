"use client";
import { useMemo, useState } from "react";
import Link from "next/link";
import type { Game } from "@/content/games";
export function GameCatalog({ games }: { games: Game[] }) { const [query,setQuery]=useState(""); const [genre,setGenre]=useState("all"); const genres=["all",...new Set(games.map(g=>g.genre))]; const filtered=useMemo(()=>games.filter(g=>`${g.title} ${g.description} ${g.tags.join(" ")}`.toLowerCase().includes(query.toLowerCase())&&(genre==="all"||g.genre===genre)),[games,genre,query]); return <><div className="mt-8 flex flex-wrap gap-3"><input aria-label="Search games" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search games" className="min-w-56 rounded-full border border-white/15 bg-black/30 px-4 py-2"/><select aria-label="Filter by genre" value={genre} onChange={e=>setGenre(e.target.value)} className="rounded-full border border-white/15 bg-black/30 px-4 py-2"><option value="all">All genres</option>{genres.slice(1).map(g=><option key={g}>{g}</option>)}</select><button type="button" onClick={()=>{if(games.length) window.location.href=`/games/${games[Math.floor(Math.random()*games.length)].slug}`}} className="rounded-full border border-cyan-300/50 px-4 py-2 text-cyan-200">Surprise me</button></div><p className="mt-4 text-sm text-slate-400" aria-live="polite">Showing {filtered.length} of {games.length} games</p><div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(g=><Link key={g.slug} href={`/games/${g.slug}`} className="group rounded-2xl border bg-card p-6 transition hover:-translate-y-1 hover:border-cyan-400"><div className="text-5xl">{g.emoji}</div><h2 className="mt-5 text-xl font-semibold group-hover:text-cyan-400">{g.title}</h2><p className="mt-2 text-sm text-muted-foreground">{g.description}</p><div className="mt-5 flex flex-wrap gap-2">{g.tags.map(t=><span key={t} className="rounded-full border px-2 py-1 text-xs">{t}</span>)}</div></Link>)}</div></> }
