{
  "name": "4weird-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal 4weird auth + user-data service. Security-only scope: login sessions in httpOnly cookies, user-scoped reads (RLS still applies), server-side Vibe Coins claims. No UI beyond a status page.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.116.0",
    "next": "16.3.4",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  }
}
